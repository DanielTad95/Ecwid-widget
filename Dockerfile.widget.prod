FROM nginx:alpine

# Copy widget files
COPY widget/ /usr/share/nginx/html/

# Copy nginx configuration for static files
COPY nginx-widget.conf /etc/nginx/nginx.conf

# Expose port
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
